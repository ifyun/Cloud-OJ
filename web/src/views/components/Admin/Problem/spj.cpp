#include <fstream>

/**
* @brief 实现此函数完成 Special Judge
*
* 没有输入数据时 in 指向 /dev/null
* 
* 此函数发生错误归类为内部错误(IE)
*
* @param in: 输入
* @param out: 用户输出
* @param ans: 正确输出
* @return @c true AC @c false WA
*/
extern "C" bool spj(std::ifstream *in, std::ifstream *out, std::ifstream *ans) {
    /*---- A + B SPJ 示例 ----*
    int a, b, r;

    *in >> a;
    *in >> b;
    *out >> r;

    if (a + b == r) {
        return true;
    }

    return false;
    *------------------------*/
}
