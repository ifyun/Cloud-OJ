<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.cloudli.managerservice.dao.ProblemDao">

    <select id="getAll" resultMap="Map.Problem, Map.Total">
        <choose>
            <when test="userId != null">
                select sql_calc_found_rows p.*, result
                from problem p
                left join (select user_id, problem_id, min(result) result
                from judge_result
                where user_id = #{userId}
                group by user_id, problem_id) jr
                on p.problem_id = jr.problem_id
            </when>
            <otherwise>
                select sql_calc_found_rows *
                from problem
            </otherwise>
        </choose>
        <if test="enable == true">
            where enable = true
        </if>
        limit #{start}, #{limit};
        select found_rows()
    </select>

    <select id="getSingle" resultMap="Map.Problem">
        select *
        from problem
        where
        <if test="enable == true">
            enable = true and
        </if>
        problem_id = #{problemId}
    </select>

    <select id="search" resultMap="Map.Problem, Map.Total">
        <choose>
            <when test="userId != null">
                select sql_calc_found_rows p.*, result
                from problem p
                left join (select user_id, problem_id, min(result) result
                from judge_result
                where user_id = #{userId}
                group by user_id, problem_id) jr
                on p.problem_id = jr.problem_id
            </when>
            <otherwise>
                select sql_calc_found_rows problem_id, title, enable, category, create_at
                from problem
            </otherwise>
        </choose>
        where
        <if test="enable == true">
            enable = true and
        </if>
        (title like concat(#{keyword}, '%') or find_in_set(#{keyword}, category))
        limit #{start}, #{limit};
        select found_rows()
    </select>

    <insert id="add" parameterType="top.cloudli.managerservice.model.Problem">
        insert into problem(title, description, input, output, sample_input, sample_output, enable, category, score)
            value (#{title}, #{description}, #{input}, #{output}, #{sampleInput}, #{sampleOutput}, #{enable}, #{category}, #{score})
    </insert>

    <update id="update" parameterType="top.cloudli.managerservice.model.Problem">
        update problem
        set title         = #{title},
            description   = #{description},
            input         = #{input},
            output        = #{output},
            sample_input  = #{sampleInput},
            sample_output = #{sampleOutput},
            enable        = #{enable},
            score         = #{score},
            category      = #{category}
        where problem_id = #{problemId}
    </update>

    <delete id="delete">
        delete
        from problem
        where problem_id = #{problemId}
    </delete>

    <update id="updateEnable">
        update problem
        set enable = #{enable}
        where problem_id = #{problemId}
    </update>
</mapper>