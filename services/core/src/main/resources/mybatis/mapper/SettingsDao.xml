<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cloud.oj.core.dao.SettingsDao">
    <select id="get" resultType="cloud.oj.core.entity.Settings">
        select *
        from settings
        where id = 0 for
        update;
    </select>
    <update id="update" parameterType="cloud.oj.core.entity.Settings">
        update settings
        set icp                      = #{icp},
            icp_url                  = #{icpUrl},
            site_name                = #{siteName},
            show_ranking_after_ended = #{showRankingAfterEnded},
            show_not_started_contest = #{showNotStartedContest}
        where id = 0;
    </update>
</mapper>