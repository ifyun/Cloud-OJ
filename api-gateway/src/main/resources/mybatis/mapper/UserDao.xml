<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.cloudli.gateway.dao.UserDao">

    <select id="findUserById" resultType="top.cloudli.gateway.model.User">
        select u.user_id, name, password, email, section, r.*
        from user u
                 join role r on u.role_id = r.role_id where user_id = #{userId};
    </select>
</mapper>