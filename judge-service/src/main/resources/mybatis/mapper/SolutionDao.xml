<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.cloudli.judgeservice.dao.SolutionDao">
    <select id="getSubmitted" resultType="top.cloudli.judgeservice.model.Solution">
        select *
        from solution
        where state = 2
    </select>

    <select id="getJudgedByUserId" resultType="top.cloudli.judgeservice.model.JudgeResult">
        select *
        from judge_result
        where user_id = #{userId}
          and state = 0
    </select>

    <select id="getJudgedBySolutionId" resultType="top.cloudli.judgeservice.model.JudgeResult">
        select *
        from judge_result
        where solution_id = #{solutionId}
          and state = 0;
    </select>

    <insert id="add" useGeneratedKeys="true" keyProperty="solutionId"
            parameterType="top.cloudli.judgeservice.model.Solution">
        insert into solution(user_id, problem_id, contest_id, language)
        values (#{userId}, #{problemId},
        <choose>
            <when test="contestId != null">#{contestId}</when>
            <otherwise>null</otherwise>
        </choose>
        , #{language})
    </insert>

    <update id="update" parameterType="top.cloudli.judgeservice.model.Solution">
        update solution
        set state     = #{state},
            result    = #{result},
            pass_rate = #{passRate}
        where solution_id = #{solutionId}
    </update>

</mapper>